<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manar.grades">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Studentinfoactivity" />
        <activity android:name=".adminActivity" />
        <activity android:name=".teacherHomePage" />
        <activity android:name=".addnewTeachers" />
        <activity android:name=".addnewStudent" />
        <activity android:name=".attendaceActivity" />
        <activity android:name=".marksActivity" />
        <activity android:name=".showmarksActivity" />
        <activity android:name=".hwandwxamActivity"></activity>
    </application>

</manifest>